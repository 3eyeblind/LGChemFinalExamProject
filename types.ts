export interface run_log {
  loop_count: number;
  run_time: number;
  array_size: number;
}

export interface run_record {
  sort: string;
  run_time: number;
  run_number: number;
  loop_count: number;
  array_size: number;
}
